{
  "buildCommand": "prisma generate && prisma migrate deploy && tsx prisma/seed.ts && next build",
  "crons": [
    {
      "path": "/api/cron/daily-report",
      "schedule": "0 5 * * *"
    }
  ]
}
