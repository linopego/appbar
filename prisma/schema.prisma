generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder — schema completo aggiunto nel prossimo prompt
model Placeholder {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
